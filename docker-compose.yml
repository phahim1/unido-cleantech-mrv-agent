version: '3.8'

services:
  api:
    build: .
    container_name: unido_mrv_api
    command: uvicorn src.ingestion.api:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1

  dashboard:
    build: .
    container_name: unido_mrv_dashboard
    command: streamlit run src/reporting/dashboard.py --server.port=8501 --server.address=0.0.0.0
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    depends_on:
      - api